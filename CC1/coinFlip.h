int** generateCoinFlipModel(int numFlips);

int getValue(int** coinFlipModel, int row, int col);

int getSumCombos(int** coinFlipModel, int numFlips);

int getPercentage(int** coinFlipModel, int numFlips, int numHeads);
